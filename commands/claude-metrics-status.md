---
description: Print Claude Metrics plugin status — endpoint, opt-out, queue depth, last send, daemon status, recent errors.
allowed-tools: Bash
---

Run the bash command below and report its output verbatim to the user.

!`node "${CLAUDE_PLUGIN_ROOT}/lib/status.mjs"`
