---
description: Stop the Claude Metrics background health-check daemon (if running).
allowed-tools: Bash
---

Run the bash command below and report its output verbatim.

!`node "${CLAUDE_PLUGIN_ROOT}/lib/healthcheck-stop.mjs"`
